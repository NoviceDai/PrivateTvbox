# 2026-07-15

## TVBox 频道列表合并
- 从 `http://192.168.8.188:50003/sub.m3u` 获取本地 IPTV 代理频道列表（201 个频道，含 `#EXTHTTP`/`#EXTKU9OPT`/`#EXTVLCOPT` 请求头指令）。
- 编写 `/tmp/merge_m3u.py`：按 `tvg-name` 归一化匹配（去空格、去"上海"前缀、去"4K"后缀、去综合/财经等后缀），把 `192.168.8.188:50003` 新地址作为播放源**第一位**插入 `Daixiao_CMCC.m3u` 中已匹配频道；sub 中不存在于 Daixiao 的频道追加到末尾。
- 结果：匹配插入 41 处，新增 148 个频道（文件 468→1191 行）。
- 注意：`sub.m3u` 部分频道带 `#EXTHTTP` 头，真实播放 URL 在头指令之后，解析时须跳过所有 `#` 头行再取 http 行。
- 原文件已备份为 `Daixiao_CMCC.m3u.bak`（未修改的原始版本）。

## 调整策略（后续）
- 用户要求：只更新已有频道，不追加新频道；且只把本地代理地址（192.168.8.188:50003）置顶，不注入 sub 中的外部地址（如凤凰系列的 php.doube.eu.org）。
- 修正脚本 `/tmp/merge_m3u.py`：sub 解析改为收集频道的**全部** http URL；合并时仅抽取含 `192.168.8.188:50003` 的本地地址置顶；无本地地址的频道（凤凰等）不改动。最终结果：38 个频道本地源置顶正确，0 新增，0 异常，文件 538 行。
- 关键坑：①sub 部分频道带 `#EXTHTTP`/`#EXTKU9OPT`/`#EXTVLCOPT` 头指令，真实 URL 在头后；②凤凰中文/资讯/香港在 sub 中地址为外部 php，非本地代理；③校验脚本收集 URL 时必须跳过以 `#` 开头的头行，否则会误判。
- 每次运行脚本会 `shutil.copy` 覆盖 `Daixiao_CMCC.m3u.bak`，重跑前务必先从 bak 还原（cp Daixiao_CMCC.m3u.bak Daixiao_CMCC.m3u）以免备份被污染。

## 测试并清理失效播放源（2026-07-15）
- 任务：测试 Daixiao_CMCC.m3u 全部播放源，删除失效链接。
- 脚本 `/tmp/test_m3u_apply.py`：解析频道结构（保留 #EXTHTTP 等头指令与对应 URL 配对），用线程池(30)并发 urllib 请求，每 URL 测 2 次（超时/HTTP错误才判死），删除死链及其配套头，整频道全死则移除。
- 结果：287 唯一 URL，删 71 失效（14 HTTP错误+58 超时），频道 184→148，移除 36 个全失效频道。本地代理源 192.168.8.188:50003 共 29 个保留（仅 2 个签名过期被删），仍居首位。
- 清理前完整版备份：`Daixiao_CMCC.m3u.testbak`（可随时 `cp Daixiao_CMCC.m3u.testbak Daixiao_CMCC.m3u` 还原）。
- 风险提示：58 个"超时"多为国际/港台源及部分上海移动源，可能只是 WSL 检测网络连不上，在用户电视网络可能可用（如河北卫视、星空卫视等）。已向用户提示并询问是否仅删 HTTP错误类、或重新拉取 sub.m3u 本地签名补回。
- 本地代理 sub.m3u 端点 192.168.8.188:50003/sub.m3u 可达（302），但 play/index.m3u8?sign= 的签名会过期，过期后该 URL 会超时被判死。
